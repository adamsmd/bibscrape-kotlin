This folder contains tests that are known to fail.

If this folder and the parent folder contain identically named files, then the
parent folder contains a *fake* test that expects the current but incorrect
result.  This folder then contains the actual correct result.
